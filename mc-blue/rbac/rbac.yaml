removeStrategy:
  rbac: SYNC   # 🔒 Enforce RBAC strictly from CasC (no UI drift)

roles:
  - name: controller-admin
    permissions:
      - hudson.model.Hudson.Administer

groups:
  - name: MC-ALPHA-ADMINS
    roles:
      - name: controller-admin
        grantedAt: current
    members:
      users:
        - admin
