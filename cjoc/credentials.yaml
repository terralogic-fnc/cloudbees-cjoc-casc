credentials:
  system:
    domainCredentials:
      - domain:
          name: "global"
        credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "casc-git-creds"
              username: "${GIT_USERNAME}"
              password: "${GIT_TOKEN}"
              description: "GitOps credentials for CJOC CasC"

