removeStrategy:
  items: NONE
  rbac: SYNC

items:
  - kind: managedController
    name: mc-alpha
    displayName: Managed Controller Alpha
    description: 🔒 Controller description managed ONLY by OC CasC

    properties:
      - healthReporting:
          enabled: false

    configuration:
      kubernetes:
        clusterEndpointId: default
        domain: mc-alpha
        cpus: "1.0"
        memory: 4096
        disk: 20
        storageClassName: nfs-client
        fsGroup: "1000"

