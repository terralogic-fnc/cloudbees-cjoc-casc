apiVersion: 2
version: "1"
id: "cjoc"
description: "CloudBees Operations Center CasC bundle"

jcasc:
  - jenkins.yaml

plugins:
  - plugins.yaml

rbac:
  - rbac.yaml

items:
  - items.yaml

removeStrategy:
  items: "sync"
  rbac: "sync"

