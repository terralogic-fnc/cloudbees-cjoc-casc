removeStrategy:
  rbac: SYNC

roles:
  - name: controller-admin
    permissions:
      - hudson.model.Hudson.Administer

groups:
  - name: Controller-Admins
    roles:
      - name: controller-admin
        grantedAt: current
    members:
      users:
        - admin

